import React from 'react';

const style = {
  backgroundColor: '#ff3300',
  position: 'absolute',
  left: 0,
  right: 0,
  top: 0,
  bottom: 0,
};

const Loader = () => <div style={style} />;

export default Loader;
